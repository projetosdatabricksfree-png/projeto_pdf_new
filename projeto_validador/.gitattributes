# Git LFS tracking for large binary fixtures
# Enable LFS if the real_batch directory total exceeds 50MB:
#   git lfs install
#   git add .gitattributes
#   git commit -m "chore: enable LFS for PDF fixtures"
tests/fixtures/real_batch/*.pdf filter=lfs diff=lfs merge=lfs -text
